<IfModule mod_rewrite.c>
  RewriteEngine On
  # KÖK: /
  # ALT KLASÖR ÖRNEK: /kaan_ucus/public
  RewriteBase /

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>
